{
  "name": "@zarclays/sapling-wasm",
  "author": "Zarclays <info@zarclays.com>",
  "version": "0.0.8",
  "homepage": "https://www.zarclays.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/zarclays/zgap-sapling"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sourceMap": true,
  "scripts": {
    "build": "rm -rf dist && npx webpack --mode=development && npm link ./dist",
    "build:prod": "rm -rf dist && npx webpack --mode=production"
  }
}
